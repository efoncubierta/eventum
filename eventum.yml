provider: AWS

aws:
  store:
    event:
      tableName: eventum-events-dev
    snapshot:
      tableName: eventum-snapshots-dev
  stream:
    event:
      streamName: eventum-events-dev
      
snapshot:
  retention:
    count: 2