provider: AWS

aws:
  store:
    journal:
      tableName: eventum-journals-test
    snapshot:
      tableName: eventum-snapshots-test
  stream:
    event:
      streamName: eventum-events-test
      
snapshot:
  retention:
    count: 2