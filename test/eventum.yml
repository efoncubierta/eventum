provider: AWS

aws:
  store:
    event:
      tableName: eventum-events-test
    snapshot:
      tableName: eventum-snapshots-test
  stream:
    event:
      streamName: eventum-events-test
      
snapshot:
  retention:
    count: 2